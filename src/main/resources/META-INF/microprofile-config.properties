# Server
server.port=8080
server.host=0.0.0.0

# App
app.greeting=Hello from config!

# Kubernetes (optional overrides)
k8s.namespace.default=default
# k8s.masterUrl=https://your-apiserver:6443  # leave empty for auto
